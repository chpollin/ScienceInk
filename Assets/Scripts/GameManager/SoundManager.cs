using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SoundManager : MonoBehaviour
{
    public static AudioClip playerHurtSound, BoilerSound, jumpSound;
    //static AuioSource audioSource;

    // Start is called before the first frame update
    void Start()
    {
         //playerHurtSound = Resource.Load<AudioClip> ("playerHurtSound");
         //BoilerSound = Resource.Load<AudioClip> ("BoilerSound");
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
